# homebrew-tap

Homebrew tap for `slash-key`.

## Install

```bash
brew tap kmatsushita1012/tap
brew install slash-key
```

## Notes

- The tap ships the initial `slash-key` formula.
- The long-term goal is to upstream the formula into Homebrew core so users can install without a tap.
